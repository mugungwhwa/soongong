/**
 * 공유 원시 타입 — 전 트랙 정합 앵커.
 *
 * Tech Lead 소유 계약. 변경은 Tech Lead 경유만 (CLAUDE.md §3 / SOO-10).
 * SSoT: 과목 라벨 체계 = **한국 수능 영역**(Mike 결정, SOO-150). 세부 선택과목
 * (생활과윤리·물리학Ⅰ 등)은 이 영역 아래 계층으로 A안 범위 밖(추후 온톨로지).
 */

/** 과목 라벨 = 수능 영역. 회독·AI·게임 트랙 간 `subject` 필드 단일 정의. */
export type Subject =
  | "국어"
  | "수학"
  | "영어"
  | "사회탐구"
  | "과학탐구"
  | "직업탐구"
  | "한국사"
  | "제2외국어/한문";

/** 표시 우선순위 순서(국·수·영·탐구 먼저) — 선택칩/진행률 렌더 기준. */
export const SUBJECTS: readonly Subject[] = [
  "국어",
  "수학",
  "영어",
  "사회탐구",
  "과학탐구",
  "직업탐구",
  "한국사",
  "제2외국어/한문",
] as const;
